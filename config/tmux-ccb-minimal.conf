# =============================================================================
# Minimal CCB (Claude Code Bridge) tmux configuration
# Only adds CCB features, does NOT change your settings
# =============================================================================

# CCB script directory (required for CCB theming)
set -g @ccb_bin_dir "/home/michael/projects/claude_code_bridge/bin"

# =============================================================================
# End of minimal CCB configuration
# =============================================================================
# NOTE: CCB theme (status bar, pane colors) is applied ONLY while CCB is running
# via ccb-tmux-on.sh and restored on exit via ccb-tmux-off.sh
# Your existing tmux theme/settings are preserved when CCB is not running
# =============================================================================
